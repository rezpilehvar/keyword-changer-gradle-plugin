plugins {
    id 'ir.irezaa.keywordchanger'
}

keywordChanger {
    configs {
        stagingToProduction {
            srcPath "src/main"
            tmpPath "$buildDir/src/production"
            renamePathAndFiles = true

            keywords = [
                    "Staging"                        : "Production",
                    "sample"                         : "app",
                    "ir.irezaa.keywordchanger.sample": "ir.irezaa.keywordchanger.app"
            ]
        }
    }
}